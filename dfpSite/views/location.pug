#where.w3-container
    .w3-content(style="max-width:700px")
        h5.w3-center.w3-padding-48
            span.w3-tag.w3-wide WHERE TO FIND US
        p Find us at 1664 E 63rd St, Kansas City, MO 64110
        p We're open 7 days a week!
        //#map

//- script. 
//-     // Initialize and add the map
//-     function initMap() {
//-         // The location of Uluru
//-         var uluru = {lat: 39.014128, lng: -94.566002};
//-         // The map, centered at Uluru
//-         var map = new google.maps.Map(
//-             document.getElementById('map'), {zoom: 4, center: uluru});
//-         // The marker, positioned at Uluru
//-         var marker = new google.maps.Marker({position: uluru, map: map});
//-     }